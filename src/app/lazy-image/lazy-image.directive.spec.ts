// import { LazyImageDirective } from "./lazy-image.directive";

describe("LazyImageDirective", () => {
  it("should create an instance", () => {
    // mock element ref
    // const directive = new LazyImageDirective();
    // expect(directive).toBeTruthy();
    expect(1).toBeTruthy();
  });
});
